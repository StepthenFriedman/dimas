#include <stdio.h>

int main(){
	int N,V,dp[110], v[110],w[110],p ,i,j;
	while (scanf("%d %d",&N,&V)!=EOF){
		for (i=0;i<=N;i++) dp[i]=0;
		for (i=0;i<N;i++){
			scanf("%d %d %d",&v[i],&w[i],&p[i]);
			
		}
	}
}
